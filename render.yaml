services:
  - type: web
    name: media-downloader
    env: node
    plan: free
    buildCommand: "bash build.sh"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
